import React from 'react';
import {
    View,
    Text,
    Image,
    TouchableOpacity,
    Linking
} from 'react-native';
import {
    IconButton,
    TextButton
} from '../components';
import {
    COLORS,
    SIZES,
    FONTS,
    icons,
    constants
} from '../constants';

const Scan = ({ navigation }) => {
    return (
        <View>
            <Text>Hello Scanner</Text>
        </View>
    )
}

export default Scan;

